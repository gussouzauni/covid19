name: api_rest1
description: Um novo projeto flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  dio: 3.0.9

dev_dependencies:
  mobx_codegen:
  build_runner:

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
